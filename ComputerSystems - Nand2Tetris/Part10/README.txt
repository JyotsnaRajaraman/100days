Project 10: Syntax Analyzer

The syntax analyzer that parses Jack programs according to the Jack grammar, producing an XML file that renders the program's structure using marked-up text. This XML will be converted to VM code next week. 

You can run the program as 

python driver.py <folder path>